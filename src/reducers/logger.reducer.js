import Actions from '../actions'

const initialState = {
    message: "",
    code: 0
};

export default function logger(state = initialState, action){
    switch(action.type){
        
        
        default: 
            return state
    }
}


